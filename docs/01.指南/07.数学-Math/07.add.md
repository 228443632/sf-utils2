---
title: add【相加】🔥
date: 2022-01-24 15:32:27
permalink: /pages/40d2b6/
article: false
---

::: note 描述
加法，两数字相加，🎉 支持浮点型计算
⚠️ 非数字的值全都当作 `0` 处理
:::

## 1.示例

```js
import butils from '@bianpengfei/utils'

console.log(butils.add(0.3, 0.8)) // 输出结果 0.11

console.log(butils.add(1.222, 8)) // 输出结果 9.222

console.log(butils.add(1, 0)) // 输出结果 1

console.log(butils.add('12342s', 1.1)) // 输出结果 1.1
```

## 2.入参说明

| 参数 | 说明       | 类型     | 是否必填 | 默认值 |
| ---- | ---------- | -------- | -------- | ------ |
| arg1 | 数字参数 1 | `Number` | 是       |        |
| arg2 | 数字参数 2 | `Number` | 是       |        |

## 3.源码

```js
import Decimal from './Decimal'

/**
 * 相加
 * @param arg1
 * @param arg2
 * @returns {number}
 */

function add(arg1, arg2) {
  return +new Decimal(+arg1 || 0).add(+arg2 || 0).toFixed()
}
```
