---
title: shuffle 【数组随机打乱】
date: 2022-01-24 15:32:27
permalink: /pages/xlekgsr68/
article: false
---

::: note 描述
洗牌算法随机  <Badge text="v3.0.10+" type="success" vertical="middle"/>
:::

## 1.示例

```javascript
import { shuffle } from '@bianpengfei/utils'

shuffle([1, 2, 3, 4, 5, 6, 7, 8]) => [5, 7, 2, 3, 1, 6, 8, 4]
```

## 2.入参说明

| 参数 | 说明               | 类型     | 是否必填 |
| ---- | ------------------ | -------- | -------- |
| array  | 原数组             | `Array`  | 是       |

## 3.源码

```js
/**
 * 洗牌算法随机
 * @param {Array} array
 * @return {*[]}
 */
function shuffle(array = []) {
  let result = [],
    random
  while (array.length > 0) {
    random = Math.floor(Math.random() * array.length)
    result.push(array[random])
    array.splice(random, 1)
  }
  return result
}

export default shuffle
```
