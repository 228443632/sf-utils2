---
title: getParentScrollElement【获取父元素滚动元素】 🔥
date: 2022-01-24 15:32:27
permalink: /pages/4adeb4/
article: false
---

::: note 描述
获取当前 dom 元素在哪个父元素下进行滚动，返回最近的 overflow: auto 的父元素 Dom。 <br/>
如果当前 Dom 元素正好是滚动视图，返回自己；否则返回最近的上级滚动的视图容器。
:::

## 1.示例

::: demo

```vue
<template>
  <div>
    <el-divider content-position="left">推荐打开控制查看 dom元素节点</el-divider>

    <div class="ex-scroll-view__wrap" style="background: palegoldenrod">
      <div style="padding: 20px 0">.ex-scroll-view__wrap 容器</div>
      <div class="ex-scroll-view">
        <div class="box-1" style="background: #dcbaba; height: 100px">box-1</div>
        <div style="background: #adad55; height: 100px">box-2</div>
        <div style="background: #4949ea; height: 100px">box-3</div>
        <div style="background: #cd2ec8; height: 100px">box-4</div>
      </div>
    </div>

    <div style="margin-top: 12px">
      <el-button type="primary" @click="getParentScrollElement('.ex-scroll-view .box-1')"
        >获取box-1的父滚动容器</el-button
      >
      <el-button type="primary" @click="getParentScrollElement('.ex-scroll-view__wrap')"
        >获取.ex-scroll-view__wrap的父滚动容器</el-button
      >
    </div>
  </div>
</template>

<script>
import butils from '@bianpengfei/utils'

export default {
  methods: {
    getParentScrollElement(cls) {
      console.log(butils.getParentScrollElement(document.querySelector(cls)))
    }
  }
}
</script>

<style>
.ex-scroll-view__wrap {
  padding: 0 20px 20px;
}
.ex-scroll-view {
  overflow-x: auto;
  overflow-y: auto;
  height: 150px;
  border: 1px solid chocolate;
}
</style>
```

:::

### 打印结果：

<br/>
<img :src="$withBase('/img/console.log-03.png')">

## 2.入参说明

| 参数          | 说明          | 类型          | 是否必填 | 默认值 |
| ------------- | ------------- | ------------- | -------- | ------ |
| targetElement | 目标 dom 元素 | `HTMLElement` | -        | -      |

## 3.源码

::: details 源码，点开查看 👈

```js
import getStyle from './getStyle'

/**
 * 获取当前元素在哪个父元素滚动视图，如果当前Dom元素正好是滚动视图，返回自己；否则返回最近的上级滚动的视图容器
 * @param {HTMLElement} targetElement 目标元素
 * @returns {{HTMLElement}}
 */
function getParentScrollElement(targetElement = null) {
  const cb = (targetElement = null, scrollView = { el: null }) => {
    if (['BODY', 'HTML'].includes(targetElement?.tagName)) {
      scrollView.el = document?.body
    } else if (
      targetElement &&
      (getStyle(targetElement, 'overflow-y') == 'auto' || getStyle(targetElement, 'overflow-x') == 'auto')
    ) {
      scrollView.el = targetElement
      scrollView.el = targetElement
    } else if (targetElement?.parentElement) {
      cb(targetElement.parentElement, scrollView)
    }
    return scrollView
  }
  const { el } = cb(targetElement)
  return el
}

export default getParentScrollElement
```

:::
