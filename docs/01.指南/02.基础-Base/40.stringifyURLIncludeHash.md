---
title: stringifyURLIncludeHash【加工URL包含Hash参数】
date: 2026-05-09 12:00:00
permalink: /pages/7d078c/
article: false
---

::: note 描述
加工 URL，支持同时处理 hash 和 search 参数 <Badge text="3.3.6-beta.3+" type="success" vertical="middle"/>
:::

## 1.示例

```js
import { stringifyURLIncludeHash } from 'sf-utils2'

stringifyURLIncludeHash('https://example.com/path#/login', {
  hash: { a: 1, token: 'abc123' },
  params: { b: 2, lang: 'zh-CN' }
})
// 输出 https://example.com/path?b=2&lang=zh-CN#/login?a=1&token=abc123
```

## 2.入参说明

| 参数    | 说明                           | 类型                              | 是否必填 |
| ------- | ------------------------------ | --------------------------------- | -------- |
| url     | URL 地址                       | `string`                          | 是       |
| options | 包含 hash 和 params 参数的对象 | `IStringifyURLIncludeHashOptions` | 否       |

### options 配置项

| 参数   | 说明                | 类型                  | 是否必填 |
| ------ | ------------------- | --------------------- | -------- |
| hash   | hash 中的对象参数   | `Record<string, any>` | 否       |
| params | search 中的对象参数 | `Record<string, any>` | 否       |

## 3.源码

```js
/**
 * stringifyURLIncludeHash 的参数选项
 */
interface IStringifyURLIncludeHashOptions {
  /** hash 的对象参数 */
  hash?: Record<string, any>

  /** params 的对象参数 */
  params?: Record<string, any>
}

/**
 * @param url
 * @param options
 */
export default function stringifyURLIncludeHash(url, options) {
  if (url && isObject(options)) {
    const globalThis = getGlobalThis()
    const URL = globalThis.URL
    let base
    if (!/^http/.test(url)) base = isNode() ? 'http://localhost' : globalThis.origin
    const urlInstance = new URL(url, base)

    const { hash: hashParams, params: historyParams } = options || {}

    if (isObject(hashParams) && urlInstance.hash) {
      const tempBase = isNode() ? 'http://localhost' : globalThis.origin
      const hashUrlInstance = new URL(urlInstance.hash.slice(1), tempBase)
      Object.entries(hashParams).forEach(([k, v]) => {
        hashUrlInstance.searchParams.set(k, v)
      })
      urlInstance.hash = '#' + hashUrlInstance.toString().replace(tempBase, '')
    }

    if (isObject(historyParams)) {
      Object.entries(historyParams).forEach(([k, v]) => {
        urlInstance.searchParams.set(k, v)
      })
    }
    if (base) return urlInstance.toString().replace(base, '')
    return urlInstance.toString()
  }
  return url
}
```
